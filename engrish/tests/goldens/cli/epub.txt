usage: engrish epub [-h] --form FORM

options:
  -h, --help   show this help message and exit
  --form FORM  '+'-separated locale codes. Recurses into generate → merge → df
               → render if needed.
